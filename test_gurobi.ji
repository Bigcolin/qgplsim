using  QuantReg, DataFrames, GLM, Gurobi

n = 100
Xn = randn(n, 2)
X = [ones(n)  Xn]

b = [-1.0, 2.0, 0.5]

y = X*b + randn(n) * 0.4
df = DataFrame(Xn)
nox = Symbol.(names(df))
df[:y] = y
formula1 = Term(:y) ~ sum([Term(x) for x in nox])

model = QuantRegModel(formula1, df, τ = 0.5)
