include("DGP.ji")
include("estimator.ji")

n = 500

function main(i, γ, θ, α, β, nSample = 500)
    X, Z, y = dgp(nSample, γ, θ, α, β)
    alpha, theta, alpha_z, theta_z = qgplsim.alphatheta_estimator(X, Z, y)
    # f = open("theta.txt", "a")
    # println(f, i, ": ", theta, "\n")
    # close(f)
    theta
end